import { css } from "@emotion/css";

const styles = css`
  color: var(--white-color);
  display: flex;
  justify-content: center;
  flex-direction: row;
  flex-wrap: wrap;
  padding: 50px 0;
`;

export default styles;
